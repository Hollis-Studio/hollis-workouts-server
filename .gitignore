node_modules/
dist/
.env
.env.*
!.env.example
coverage/
.tsbuildinfo
*.log
prisma/generated/

# Terraform — provider/plugin cache and any real (secret-bearing) tfvars.
infrastructure/.terraform/
*.tfvars
!*.tfvars.example
# Terraform state holds secrets/ARNs — never commit it.
*.tfstate
*.tfstate.*
# Rendered ECS task def may contain real ARNs/account IDs.
ops/*.rendered.json
